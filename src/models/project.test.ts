describe("Project Model", () => {
    it("can add projects (addProject)", () => {
        //
    });

    it("can find projects (getProject)", () => {
        //
    });

    it("can remove projects (removeProject)", () => {
        //
    });

    it("can get random projects (getRandomProject)", () => {
        //
    });
});
